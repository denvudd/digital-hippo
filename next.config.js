/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      "digital-hippo-production-denvudd.up.railway.app"
    ]
  },
};

module.exports = nextConfig;
